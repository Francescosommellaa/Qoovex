# Historical Production baseline observation — 2026-08-03

This immutable technical record proves that Production was observed read-only at migration `20260720010000_calendar_events` on 2026-08-03. It is not a description of the current repository, a release instruction, or an authorization to change a remote environment.

The current local migration chain and checksums are authoritative only in `ops/migration-ledger.json`. This record must not be used to reconstruct removed product surfaces or infer remote alignment after its observation date.

Any future Preview or Production change remains workflow-only and requires separate authorization, exact target identity, verified backup and `prisma migrate deploy`.
