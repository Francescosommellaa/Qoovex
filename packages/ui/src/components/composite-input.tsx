// Canonical composition entrypoint. The existing InputGroup implementation is
// retained for compatibility; there is only one boundary and state language.
export { InputGroup as CompositeInput, InputAddon } from "./input"
