# UI Animations

Reusable motion primitives for Qoovex interfaces.

Import `@qoovex/ui/animations/motion` after tokens and base styles. The classes use the design-system timing tokens and respect `prefers-reduced-motion`.

