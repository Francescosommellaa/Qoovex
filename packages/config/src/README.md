# Config Source

Scopo: export TypeScript delle configurazioni condivise.

Metti qui:
- wrapper tipizzati per file config canonici.

Non mettere qui:
- logica applicativa;
- override locali dei limiti prodotto.

Regole:
- `plan_rules.json` resta la fonte canonica dei piani.
