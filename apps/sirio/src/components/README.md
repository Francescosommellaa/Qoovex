# Sirio Components

Nessun componente app-local e' approvato in questa fase.

La pagina Sirio usa markup statico in `app/page.tsx` per evitare di trasformare
il laboratorio in una seconda libreria UI.

