## Home Page

Cartella della homepage marketing.

Contiene:
- `home.tsx` come entrypoint di composizione;
- `sections/` per sezioni specifiche della home;
- `content/` per testi e metadata;
- `animation/` per placeholder animazioni dedicate.
