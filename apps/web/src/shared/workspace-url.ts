/** Origine canonica del Qoovex Workspace — unica fonte per link marketing → app */
export const WORKSPACE_ORIGIN = "https://app.qoovex.com" as const;

export const workspaceSignInHref = `${WORKSPACE_ORIGIN}/sign-in` as const;
export const workspaceSignUpHref = `${WORKSPACE_ORIGIN}/sign-up` as const;
