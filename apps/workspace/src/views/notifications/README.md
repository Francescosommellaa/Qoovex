# Notifications View

Scopo: schermata inbox notifiche del workspace autenticato.

Usa:
- service server per dati e filtri iniziali;
- feature `notifications` per interazioni client;
- `WorkspacePage` per layout compatto coerente con shell.
