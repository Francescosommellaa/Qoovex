# Menus View

Scopo: composizione delle schermate legate ai menu.

Metti qui:
- pagine lista menu, dettaglio o builder shell quando la schermata cresce.

Non mettere qui:
- dominio `Menu` puro;
- flow utente atomici del builder.

Regole:
- usa `features/menu-builder` per l'interazione;
- usa `entities/menu` per il modello.
