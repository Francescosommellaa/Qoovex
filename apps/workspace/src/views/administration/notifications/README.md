# Notifications Views

Componenti app-local per la pagina `/notifications`.

Mostrano promemoria interni derivati da dati registrati e usano solo API protette. Non espongono riferimenti Blob, token, hash token o URL permanenti.
