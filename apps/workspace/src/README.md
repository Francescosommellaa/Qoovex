# Workspace Source

Baseline API-only:
- `app/api` contiene i controller HTTP;
- `shared/server` contiene servizi e repository;
- `shared/lib` e `shared/config` contengono contratti app-local;
- `proxy.ts` protegge le API.

I layer frontend FSD verranno ricreati solo insieme alla nuova esperienza.
