import Link from "next/link";
import { getOrganizationJobSiteDetail } from "@shared/server/job-site-lifecycle-service";
import { getOrganizationPaymentProfile } from "@shared/server/job-site-payment-profile-service";
import { buttonVariants } from "@qoovex/ui/components/button";
import { WorkspaceEmptyState, WorkspacePage, WorkspacePageHeader, WorkspacePanel, WorkspaceState } from "@/views/workspace/WorkspacePrimitives";
import { ActionButton, AgreementForm, AttachmentForm, AuthorityGrantForm, CollaboratorInviteForm, DeleteActionButton, DisputeForm, InviteClientForm, LegalHoldForm, ParticipantForm, PaymentRequestForm, PaymentReviewForm, PostClosureForm, ProposalCounterForm, ProposalForm, RecordTransitionForm, ReopeningForm, ReleaseLegalHoldButton, RequestForm, StepForm, TimelineForm } from "@/views/job-site/JobSiteForms";
import { JobSiteSearch } from "@/views/job-site/JobSiteSearch";

const sections = ["riepilogo", "timeline", "step", "richieste", "modifiche", "pagamenti", "persone", "file", "chiusura", "impostazioni"] as const;
function date(value: Date | string | null | undefined) { return value ? new Intl.DateTimeFormat("it-IT", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) : "Non indicata"; }
function organizationStepActions(status: string, jobSiteStatus: string) {
  if (jobSiteStatus !== "ACTIVE") return status === "NOT_STARTED" ? [{ value: "CANCELLED", label: "Annulla" }] : [];
  if (status === "NOT_STARTED") return [{ value: "IN_PROGRESS", label: "Avvia" }, { value: "WAITING", label: "Metti in attesa" }];
  if (["IN_PROGRESS", "CHANGES_REQUESTED"].includes(status)) return [{ value: "WAITING", label: "Metti in attesa" }, { value: "WORK_COMPLETED", label: "Pronto per controllo" }];
  if (status === "WAITING") return [{ value: "IN_PROGRESS", label: "Riprendi" }, { value: "WORK_COMPLETED", label: "Pronto per controllo" }];
  return [];
}

export default async function OrganizationJobSiteDetail({ params }: { params: Promise<{ organizationId: string; jobSiteId: string }> }) {
  const { organizationId, jobSiteId } = await params; const [site, paymentProfile] = await Promise.all([getOrganizationJobSiteDetail(organizationId, jobSiteId), getOrganizationPaymentProfile(organizationId).catch(() => null)]); const base = `/api/org/${organizationId}/job-sites/${jobSiteId}`;
  return <WorkspacePage><WorkspacePageHeader title={site.name} description={site.address ?? "Indirizzo non indicato"} action={<WorkspaceState label={site.status} />} /><nav aria-label="Sezioni cantiere" className="flex gap-2 overflow-x-auto pb-1">{sections.map((section) => <a className={buttonVariants({ variant: "outline", size: "sm" })} href={`#${section}`} key={section}>{section[0]!.toUpperCase() + section.slice(1)}</a>)}</nav>
    <section id="riepilogo"><WorkspacePanel title="Riepilogo" description={`Revisione ${site.revision}`}><dl className="grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-muted-foreground">Descrizione</dt><dd>{site.description ?? "Non indicata"}</dd></div><div><dt className="text-muted-foreground">Conclusione stimata</dt><dd>{date(site.estimatedCompletionAt)}</dd></div><div><dt className="text-muted-foreground">Creato</dt><dd>{date(site.createdAt)}</dd></div><div><dt className="text-muted-foreground">Chiuso</dt><dd>{date(site.closedAt)}</dd></div></dl></WorkspacePanel></section>
    <section id="timeline" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Timeline" description="Registro append-only; gli eventi interni non sono visibili al cliente.">{site.timelineEvents.length ? <ol className="space-y-4">{site.timelineEvents.map((event) => <li className="border-l-2 pl-4" key={event.id}><div className="flex flex-wrap items-center gap-2"><strong>{event.type}</strong><WorkspaceState label={event.audience} /><span className="text-xs text-muted-foreground">#{event.sequence.toString()} · {date(event.createdAt)}</span></div><pre className="mt-2 whitespace-pre-wrap text-xs text-muted-foreground">{JSON.stringify(event.payload, null, 2)}</pre></li>)}</ol> : <WorkspaceEmptyState title="Timeline vuota" description="Pubblica il primo aggiornamento reale." />}</WorkspacePanel><WorkspacePanel><TimelineForm endpoint={`${base}/timeline`} revision={site.revision} /></WorkspacePanel></section>
    <section id="step" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Step opzionali" description="L'avanzamento deriva dagli step solo quando esistono.">{site.steps.length ? <ul className="divide-y">{site.steps.map((step) => <li className="py-3" key={step.id}><div className="flex items-center justify-between"><strong>{step.title}</strong><WorkspaceState label={step.status} /></div><p className="text-sm text-muted-foreground">{step.description ?? "Nessuna descrizione"}</p>{organizationStepActions(step.status, site.status).length ? <div className="mt-2 flex flex-wrap gap-2">{organizationStepActions(step.status, site.status).map((action) => <ActionButton endpoint={`${base}/actions`} body={{ action: "STEP_STATUS_TRANSITION@1", expectedRevision: site.revision, stepId: step.id, nextStatus: action.value }} key={action.value} label={action.label} success="Stato step aggiornato." confirmMessage={action.value === "CANCELLED" ? "Annullare questo step prima dell'attivazione?" : undefined} />)}</div> : null}</li>)}</ul> : <p className="text-sm text-muted-foreground">Questo cantiere non usa step.</p>}</WorkspacePanel><WorkspacePanel>{["DRAFT", "WAITING_FOR_CLIENT", "PENDING_INITIAL_CONFIRMATION"].includes(site.status) ? <StepForm endpoint={`${base}/steps`} revision={site.revision} /> : <p className="text-sm text-muted-foreground">Dopo l'attivazione, nuovi step e variazioni strutturali passano da una proposta versionata.</p>}</WorkspacePanel></section>
    <section id="richieste" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Richieste">{site.requests.length ? <ul className="divide-y">{site.requests.map((request) => <li className="py-3" key={request.id}><div className="flex items-center justify-between"><strong>{request.title}</strong><WorkspaceState label={request.status} /></div><p className="text-sm text-muted-foreground">{request.body}</p><RecordTransitionForm endpoint={`${base}/requests/${request.id}`} revision={site.revision} actions={[{ value: "RESPOND", label: "Rispondi" }, { value: "RESOLVE", label: "Risolvi" }, { value: "WITHDRAW", label: "Ritira" }]} /></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessuna richiesta aperta.</p>}</WorkspacePanel><WorkspacePanel><RequestForm endpoint={`${base}/requests`} revision={site.revision} /></WorkspacePanel></section>
    <section id="modifiche" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Modifiche versionate">{site.changeProposals.length ? <ul className="divide-y">{site.changeProposals.map((proposal) => <li className="py-3" key={proposal.id}><div className="flex items-center justify-between"><span>Proposta {proposal.currentVersion?.version ?? "bozza"}</span><WorkspaceState label={proposal.status} /></div>{["PROPOSED", "COUNTERED"].includes(proposal.status) && proposal.currentVersion ? <div className="mt-3 space-y-3">{proposal.representedSide === "CLIENT" ? <><div className="flex gap-2"><ActionButton endpoint={`${base}/actions`} body={{ action: "CHANGE_PROPOSAL_APPLY@1", expectedRevision: site.revision, proposalId: proposal.id, versionId: proposal.currentVersion.id, decision: "ACCEPTED" }} label="Accetta" success="Proposta accettata." confirmMessage="Accetti questa precisa versione?" /><ActionButton endpoint={`${base}/actions`} body={{ action: "CHANGE_PROPOSAL_APPLY@1", expectedRevision: site.revision, proposalId: proposal.id, versionId: proposal.currentVersion.id, decision: "REJECTED" }} label="Rifiuta" success="Proposta rifiutata." /></div><ProposalCounterForm endpoint={`${base}/proposals/${proposal.id}`} revision={site.revision} currentVersion={proposal.currentVersion.version} /></> : <DeleteActionButton endpoint={`${base}/proposals/${proposal.id}`} body={{ expectedRevision: site.revision, expectedCurrentVersion: proposal.currentVersion.version }} label="Ritira" success="Proposta ritirata." confirmMessage="Ritirare questa versione della proposta?" />}</div> : null}</li>)}</ul> : <p className="text-sm text-muted-foreground">Nessuna modifica proposta.</p>}</WorkspacePanel><WorkspacePanel><ProposalForm endpoint={`${base}/proposals`} revision={site.revision} side="ORGANIZATION_MEMBER" /></WorkspacePanel></section>
    <section id="pagamenti" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Pagamenti documentati" description="Qoovex non incassa né trasferisce denaro.">{site.paymentRequests.length ? <ul className="divide-y">{site.paymentRequests.map((payment) => <li className="py-3" key={payment.id}><div className="flex items-center justify-between"><div><strong>{payment.reason}</strong><p className="text-sm text-muted-foreground">{payment.amountMinor.toString()} minor unit</p></div><WorkspaceState label={payment.status} /></div>{["TRANSFER_DECLARED", "UNDER_REVIEW"].includes(payment.status) ? <PaymentReviewForm endpoint={`${base}/actions`} revision={site.revision} paymentRequestId={payment.id} /> : null}</li>)}</ul> : <p className="text-sm text-muted-foreground">Nessuna richiesta di pagamento.</p>}<div className="mt-4"><Link className={buttonVariants({ variant: "outline" })} href={`/org/${organizationId}/payment-profile`}>Profilo pagamento</Link></div></WorkspacePanel><WorkspacePanel><PaymentRequestForm endpoint={`${base}/actions`} revision={site.revision} paymentProfileId={paymentProfile?.id ?? null} /></WorkspacePanel></section>
    <section id="persone" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Persone">{site.participants.length ? <ul className="divide-y">{site.participants.map((participant) => <li className="py-3" key={participant.id}><div className="flex items-start justify-between gap-3"><div><strong>{[participant.user.firstName, participant.user.lastName].filter(Boolean).join(" ") || "Account Qoovex"}</strong><p className="text-sm text-muted-foreground">{participant.kind} · {participant.publicRoleLabel ?? "Ruolo non indicato"} · {participant.status}</p></div>{participant.kind === "ORGANIZATION_MEMBER" && participant.id !== site.responsibleParticipantId ? <div className="flex flex-wrap gap-2"><ActionButton endpoint={`${base}/participants`} method="PATCH" body={{ participantId: participant.id, expectedRevision: site.revision }} label="Rendi responsabile" success="Responsabile aggiornato." confirmMessage="Assegnare a questa persona la responsabilità corrente del cantiere?" /><DeleteActionButton endpoint={`${base}/participants`} body={{ participantId: participant.id, reason: "Partecipazione terminata dal responsabile del cantiere." }} label="Termina" success="Partecipazione terminata." confirmMessage="Terminare l'accesso di questo Collaborator al cantiere?" /></div> : participant.kind === "CLIENT" ? <div className="flex flex-wrap gap-2">{participant.status === "ACTIVE" ? <ActionButton endpoint={`${base}/client-participation`} body={{ participantId: participant.id, expectedRevision: site.revision, action: "SUSPEND", reason: "Accesso cliente sospeso dall'Azienda con tracciamento." }} label="Sospendi" success="Accesso cliente sospeso." confirmMessage="Sospendere l'accesso del cliente?" /> : participant.status === "SUSPENDED" ? <ActionButton endpoint={`${base}/client-participation`} body={{ participantId: participant.id, expectedRevision: site.revision, action: "REACTIVATE", reason: "Accesso cliente riattivato dall'Azienda con tracciamento." }} label="Riattiva" success="Accesso cliente riattivato." /> : null}<ActionButton endpoint={`${base}/client-participation`} body={{ participantId: participant.id, expectedRevision: site.revision, action: "REVOKE", reason: "Partecipazione cliente revocata definitivamente dall'Azienda." }} label="Revoca" success="Partecipazione cliente revocata." confirmMessage="Revocare definitivamente questa partecipazione cliente?" /></div> : null}</div></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessun partecipante attivo.</p>}</WorkspacePanel><WorkspacePanel><ParticipantForm endpoint={`${base}/participants`} memberships={site.availableMemberships.map((membership) => ({ id: membership.id, label: [membership.user.firstName, membership.user.lastName].filter(Boolean).join(" ") || membership.user.email }))} /><div className="mt-6"><CollaboratorInviteForm endpoint={`${base}/collaborator-invitations`} /></div></WorkspacePanel></section>
    <section id="file" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="File">{site.attachments.length ? <ul className="divide-y">{site.attachments.map((file) => <li className="flex items-center justify-between gap-3 py-3" key={file.id}><div><strong>{file.originalFileName}</strong><p className="text-sm text-muted-foreground">{file.category} · {file.size} byte</p></div><a className={buttonVariants({ variant: "outline", size: "sm" })} href={`${base}/attachments/${file.id}/download`}>Scarica</a></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessun file pubblicato nel cantiere.</p>}<div className="mt-6"><JobSiteSearch endpoint={`${base}/search`} /></div></WorkspacePanel><WorkspacePanel><AttachmentForm endpoint={`${base}/attachments`} revision={site.revision} /></WorkspacePanel></section>
    <section id="chiusura" className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Chiusura reciproca">{site.closures.length ? <ul className="divide-y">{site.closures.map((closure) => <li className="py-3" key={closure.id}><div className="flex items-center justify-between"><span>Proposta del {date(closure.proposedAt)}</span><WorkspaceState label={closure.status} /></div>{closure.status === "CLIENT_CONFIRMED" ? <div className="mt-2"><ActionButton endpoint={`${base}/actions`} body={{ action: "JOB_SITE_CLOSE@1", expectedRevision: site.revision, closureId: closure.id, decision: "ACCEPTED" }} label="Conferma chiusura finale" success="Chiusura registrata dalle parti." confirmMessage="Confermi lo snapshot già controllato dal cliente?" /></div> : null}</li>)}</ul> : <p className="text-sm text-muted-foreground">Nessuna chiusura proposta.</p>}<div className="mt-4 flex flex-wrap gap-2">{["CLOSED", "ARCHIVED"].includes(site.status) ? <ActionButton endpoint={`${base}/actions`} body={{ action: "JOB_SITE_EXPORT_CREATE@1", expectedRevision: site.revision, audience: "ORGANIZATION" }} label="Prepara export Azienda" success="Export accodato." /> : null}{site.status === "CLOSED" ? <ActionButton endpoint={`${base}/actions`} body={{ action: "JOB_SITE_ARCHIVE@1", expectedRevision: site.revision }} label="Archivia" success="Cantiere archiviato." confirmMessage="Archiviare il cantiere chiuso in modalità read-only?" /> : null}</div></WorkspacePanel><WorkspacePanel>{site.status === "ACTIVE" ? <ActionButton endpoint={`${base}/closure`} body={{ expectedRevision: site.revision }} label="Proponi chiusura" success="Chiusura proposta." confirmMessage="Confermi che non risultano attività condivise aperte?" /> : <p className="text-sm text-muted-foreground">La chiusura può essere proposta soltanto da un cantiere attivo.</p>}</WorkspacePanel></section>
    {["CLOSED", "ARCHIVED"].includes(site.status) ? <section aria-label="Post-chiusura" className="grid gap-4 lg:grid-cols-2"><WorkspacePanel title="Richieste post-chiusura">{site.postClosureRequests.map((request) => <div className="border-b py-3" key={request.id}><div className="flex items-center justify-between"><strong>{request.title}</strong><WorkspaceState label={request.status} /></div>{["OPEN", "IN_DISCUSSION"].includes(request.status) ? <RecordTransitionForm endpoint={`${base}/post-closure/${request.id}`} revision={site.revision} actions={[{ value: "RESPOND", label: "Rispondi" }, { value: "RESOLVE", label: "Risolvi" }, { value: "WITHDRAW", label: "Ritira" }, { value: "CLOSE_WITHOUT_AGREEMENT", label: "Chiudi senza accordo" }]} /> : null}</div>)}<PostClosureForm endpoint={`${base}/post-closure`} revision={site.revision} /></WorkspacePanel><WorkspacePanel title="Riapertura reciproca">{site.reopeningProposals.map((proposal) => <div className="border-b py-3" key={proposal.id}><div className="flex items-center justify-between"><span>{proposal.reason}</span><WorkspaceState label={proposal.status} /></div>{["PROPOSED", "COUNTERPARTY_CONFIRMED"].includes(proposal.status) ? <div className="mt-2 flex gap-2"><ActionButton endpoint={`${base}/actions`} body={{ action: "JOB_SITE_REOPEN@1", expectedRevision: site.revision, reopeningProposalId: proposal.id, decision: "ACCEPTED" }} label="Conferma" success="Consenso alla riapertura registrato." /><ActionButton endpoint={`${base}/actions`} body={{ action: "JOB_SITE_REOPEN@1", expectedRevision: site.revision, reopeningProposalId: proposal.id, decision: "REJECTED" }} label="Rifiuta" success="Riapertura rifiutata." /></div> : null}</div>)}<ReopeningForm endpoint={`${base}/reopening`} revision={site.revision} /></WorkspacePanel></section> : null}
    <section id="impostazioni" className="grid gap-4 lg:grid-cols-2"><WorkspacePanel title="Cliente principale">{site.clientInvitations.map((invitation) => <div className="mb-3 flex items-center justify-between gap-3 border-b pb-3" key={invitation.id}><div><strong>{invitation.emailNormalized}</strong><p className="text-sm text-muted-foreground">Scade {date(invitation.expiresAt)}</p></div><DeleteActionButton endpoint={`${base}/client-invitations`} body={{ invitationId: invitation.id, expectedRevision: site.revision }} label="Revoca invito" success="Invito revocato." confirmMessage="Revocare questo invito cliente?" /></div>)}<InviteClientForm endpoint={`${base}/client-invitations`} revision={site.revision} /></WorkspacePanel><WorkspacePanel><AgreementForm endpoint={`${base}/agreement`} revision={site.revision} name={site.name} address={site.address} description={site.description} participants={site.participants.map((value) => ({ id: value.id, publicRoleLabel: value.publicRoleLabel }))} /></WorkspacePanel><WorkspacePanel title="Dispute" description="Qoovex conserva le dichiarazioni ma non decide chi ha ragione.">{site.disputes.filter((value) => ["OPEN", "IN_DISCUSSION"].includes(value.status)).map((dispute) => <div className="border-b py-3" key={dispute.id}><div className="flex items-center justify-between"><strong>{dispute.title}</strong><WorkspaceState label={dispute.status} /></div><RecordTransitionForm endpoint={`${base}/disputes/${dispute.id}`} revision={site.revision} actions={[{ value: "RESPOND", label: "Rispondi" }, { value: "AGREE", label: "Registra accordo" }, { value: "CLOSE_WITHOUT_AGREEMENT", label: "Chiudi senza accordo" }, { value: "WITHDRAW", label: "Ritira" }]} /></div>)}<DisputeForm endpoint={`${base}/disputes`} revision={site.revision} /></WorkspacePanel><WorkspacePanel title="Deleghe economiche" description="Owner e responsabile non ricevono autorità economica implicitamente.">{site.authorityGrants.length ? <ul className="divide-y">{site.authorityGrants.map((grant) => <li className="flex items-center justify-between gap-3 py-2" key={grant.id}><span>{grant.capability}</span><DeleteActionButton endpoint={`${base}/authority`} body={{ grantId: grant.id, expectedRevision: site.revision }} label="Revoca" success="Delega revocata." confirmMessage="Revocare immediatamente questa autorità?" /></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessuna delega attiva.</p>}<AuthorityGrantForm endpoint={`${base}/authority`} revision={site.revision} participants={site.participants.filter((value) => value.kind === "ORGANIZATION_MEMBER").map((value) => ({ id: value.id, label: [value.user.firstName, value.user.lastName].filter(Boolean).join(" ") || value.publicRoleLabel || "Collaborator" }))} /></WorkspacePanel><WorkspacePanel title="Processi del cantiere" description="Stato visibile nel contesto naturale, senza Centro operativo generico.">{site.processes.length ? <ul className="divide-y">{site.processes.map((process) => <li className="py-3" key={process.id}><div className="flex items-center justify-between gap-3"><strong>{process.definitionKey}</strong><WorkspaceState label={process.status} /></div><p className="text-sm text-muted-foreground">{process.steps.map((step) => `${step.name}: ${step.status}`).join(" · ")}</p></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessun processo persistente avviato.</p>}</WorkspacePanel></section>
    <section aria-label="Conservazione" className="grid gap-4 lg:grid-cols-2"><WorkspacePanel title="Conservazione bloccata" description="Non amplia la visibilità e impedisce cleanup distruttivi dei contenuti collegati.">{site.legalHolds.length ? <ul className="space-y-3">{site.legalHolds.map((hold) => <li className="rounded-md border p-3" key={hold.id}><div className="flex items-start justify-between gap-3"><div><strong>{hold.status}</strong><p className="text-sm text-muted-foreground">{hold.reason}</p></div>{hold.status === "ACTIVE" ? <ReleaseLegalHoldButton endpoint={`${base}/legal-holds`} holdId={hold.id} /> : null}</div></li>)}</ul> : <p className="text-sm text-muted-foreground">Nessun blocco di conservazione attivo.</p>}</WorkspacePanel><WorkspacePanel><LegalHoldForm endpoint={`${base}/legal-holds`} /></WorkspacePanel></section>
  </WorkspacePage>;
}
