import { getOrganizationPaymentProfile } from "@shared/server/job-site-payment-profile-service";
import { WorkspacePage, WorkspacePageHeader, WorkspacePanel } from "@/views/workspace/WorkspacePrimitives";
import { PaymentProfileForm } from "@/views/job-site/JobSiteForms";
export default async function PaymentProfilePage({ params }: { params: Promise<{ organizationId: string }> }) { const { organizationId } = await params; const profile = await getOrganizationPaymentProfile(organizationId); return <WorkspacePage><WorkspacePageHeader title="Profilo pagamento" description="L'IBAN è cifrato con un key ring dedicato. La modifica richiede MFA attiva e soddisfatta." /><WorkspacePanel title="Profilo corrente">{profile?.currentVersion ? <p className="text-sm">{profile.currentVersion.accountHolder} · IBAN •••• {profile.currentVersion.ibanLast4}</p> : <p className="text-sm text-muted-foreground">Nessun profilo configurato.</p>}</WorkspacePanel><WorkspacePanel><PaymentProfileForm endpoint={`/api/org/${organizationId}/payment-profile`} revision={profile?.revision ?? null} /></WorkspacePanel></WorkspacePage>; }
