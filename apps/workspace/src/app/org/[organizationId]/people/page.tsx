import { getOrganizationPeople } from "@shared/server/vnext-organization-member-service";
import { WorkspacePage, WorkspacePageHeader, WorkspacePanel, WorkspaceState } from "@/views/workspace/WorkspacePrimitives";
import { CollaboratorInviteForm } from "@/views/vnext/VNextForms";
export default async function OrganizationPeoplePage({ params }: { params: Promise<{ organizationId: string }> }) { const { organizationId } = await params; const people = await getOrganizationPeople(organizationId); return <WorkspacePage><WorkspacePageHeader title="Collaboratori" description="OWNER e COLLABORATOR sono ruoli Azienda. CLIENT resta una partecipazione separata al singolo cantiere." /><div className="grid gap-4 lg:grid-cols-[1fr_22rem]"><WorkspacePanel title="Membri Azienda">{people.members.map((member) => <div className="flex items-center justify-between gap-3 border-b py-3 last:border-0" key={member.id}><div><strong>{[member.user.firstName, member.user.lastName].filter(Boolean).join(" ") || member.user.email}</strong><p className="text-sm text-muted-foreground">{member.user.email}</p></div><WorkspaceState label={member.role} /></div>)}</WorkspacePanel><WorkspacePanel><CollaboratorInviteForm endpoint={`/api/org/${organizationId}/members`} /></WorkspacePanel></div><WorkspacePanel title="Inviti attivi">{people.invitations.length ? people.invitations.map((invitation) => <p className="py-2 text-sm" key={invitation.id}>{invitation.email}</p>) : <p className="text-sm text-muted-foreground">Nessun invito attivo.</p>}</WorkspacePanel></WorkspacePage>; }
