# /api/document-packages/[packageId]/share-links/[shareLinkId]

Revoca di un link di condivisione.

- `DELETE`: imposta `revokedAt`.

Il link resta registrato ma non e piu valido per l'accesso viewer.
