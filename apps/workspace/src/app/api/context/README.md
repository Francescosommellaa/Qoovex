# Workspace access context

Restituisce ruolo, struttura, permessi e sessione supporto derivati lato server.

Durante una sessione dev locale firmata, `company.role` e `permissions` riflettono il ruolo simulato; la membership persistita resta invariata.
