# Support API

Accesso riservato a `SUPPORT_AGENT` e `PLATFORM_ADMIN` con MFA e sessione temporanea. Il codice Azienda e soltanto un identificatore; ogni operazione mantiene l'identita Qoovex e viene auditata.
