# Tutti i cantieri

`/job-sites/all` e l'elenco completo paginato e filtrato sul server. Ricerca nome, committente e indirizzo; combina fase operativa e un segnale di attenzione senza query per riga.
