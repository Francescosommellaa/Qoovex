# Recipe Card Widget

Scopo: card composita dedicata alle ricette.

Metti qui:
- presentazione di ricette con meta, azioni e stato UI combinato.

Non mettere qui:
- editor ricetta;
- primitive base del dominio.

Regole:
- usa `entities/recipe` per gli elementi di dominio;
- niente logica di pagina o routing qui dentro.
