# Contratto canonico Qoovex

Ordine: `HowToUse.md`, `project_brain.json`, Brain MCP, `OperationalProtocol.md`, quindi `00`–`08`.

Tassonomia obbligatoria:

- `verified_current_state`: foundation-only dopo D-VNEXT-46;
- `approved_product_direction`: Qoovex vNext;
- `conceptual_not_implemented`: tutte le capacità prodotto vNext;
- `open_decision` e `hard_stop`: decisioni non autorizzate.

Le migration storiche sono immutabili. La migration `20260802010000_remove_legacy_product_foundation` elimina il precedente dominio soltanto in locale; nessun ambiente remoto è stato toccato.
