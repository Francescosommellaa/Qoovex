# Skill governance scripts

Repository-owned Node.js tooling for deterministic skill registry validation, routing, runtime evidence and update checks. Ordinary doctor/tests must remain offline. Runtime state belongs under `.codex-runtime/skill-governance/` and is never committed.
