# Skill governance configuration

`registry.json` is the machine-readable authority map for Qoovex skills. It defines ownership, dependencies, routing triggers and update policy. Qoovex canonical product/repository sources always outrank this registry.
